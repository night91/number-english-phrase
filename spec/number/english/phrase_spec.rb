require "spec_helper"

RSpec.describe Number::English::Phrase do
  it "has a version number" do
    expect(Number::English::Phrase::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
